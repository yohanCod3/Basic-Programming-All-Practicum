#include <stdio.h>

int main()
{
    int angka2 = 10;            // Nama variabel tidak boleh diawali angka
    float tinggi_badan = 170.5; // Gunakan titik (.) untuk desimal, bukan koma
    char nama_lengkap = 'B';    // Gunakan tanda kutip tunggal yang benar

    return 0;
}
