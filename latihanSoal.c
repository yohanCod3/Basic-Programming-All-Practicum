#include <stdio.h>

int main()
{
    // SOAL ARRAY 1.0
    int array[5] = {10, 20, 30, 40, 50};
    for (int i = 0; i < 5; i++)
    {
        printf("angka[%d] = %d\n", i, array[i]);
    }

    return 0;
}